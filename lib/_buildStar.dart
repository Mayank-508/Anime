import 'package:flutter/cupertino.dart';

class buildStar extends StatefulWidget {
  final int index;
  const buildStar({super.key, required this.index});

  @override
  State<buildStar> createState() => _buildStarState();
}

class _buildStarState extends State<buildStar> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
